# Route-Planner
Smart Route Planner is a DSA-based Java project that finds                 the shortest path between cities using Graph, BFS, DFS,                 Dijkstra's Algorithm, Stack, Queue and ArrayList.
